## APP CONFIG
application:
  title: Clinic API (PROD) =>

## SPRING CONFIG
spring:
  sql:
    init:
      mode: never

## ACTUATOR CONFIG
management:
  endpoints:
    web:
      exposure:
        include: health, info

## SPRING DOCS CONFIG
springdoc:
  swagger-ui:
    enabled: false
  api-docs:
    enabled: false